package puzzlesolver;

public class GameBoard {
	
	private GameSquare[][] squares;
	
	public GameBoard(int size, Color darkColor, Color lightColor) {
		//create a 2d array that is a size by size
		squares = new GameSquare [size][size];
		//using nested loops (row and column)
			//in inner loop compute required color
	}

	
	public GameSquare getGameSquare(Point aPoint) {
		
	}
	
	
}
